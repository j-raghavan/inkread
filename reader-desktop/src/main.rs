//! `reader-desktop` — host harness driving the policy via `mock_desktop` (RR1-AC3).
//!
//! Fleshed out in the harness commit; the scaffold just proves the workspace links.

fn main() {
    println!("inkread reader-desktop harness (scaffold)");
}
