<?xml version="1.0" encoding="utf-8"?>
<!-- The inkread Android shell (RR1-FR2): the UI, the EPD adapter, and the pen/touch path. All
     device, panel, and vendor specifics live on this side of the JNI bridge — the Rust core never
     names a vendor (IR-7), which `scripts/check-vendor-neutral.sh` enforces. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Opt-in online dictionary fallback (Wiktionary) for non-bundled languages (RR12 / D4). -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- In-app self-update (ADR-INKREAD-0014): a sideloaded build has no store to push fixes, so it
         checks GitHub Releases and installs a newer signed APK via PackageInstaller. The OS still
         shows its own final install confirmation (no device-owner ⇒ no silent install). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- All-files access (ADR-INKREAD-0005): the PDF export writes the annotated copy into a
         public, Partner-synced folder (e.g. /storage/emulated/0/EXPORT) so it reaches the desktop.
         Granted via Settings → All files access (Android 11+); sideloaded-app scope. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Supernote Digest ("Knowledge") write-through (ADR-INKREAD-0010): a lasso selection can be
         saved into the firmware Digest app via its exported ContentProvider
         (content://com.ratta.supernote.knowledge.provider). Both permissions are protectionLevel
         "normal", so they are auto-granted to this sideloaded app at install — no platform signature
         or system UID required. Vendor specifics stay in DigestController (IR-7). -->
    <uses-permission android:name="com.ratta.supernote.knowledge.permission.READ_KNOWLEDGE" />
    <uses-permission android:name="com.ratta.supernote.knowledge.permission.WRITE_KNOWLEDGE" />

    <!-- Package visibility (Android 11+): the post-add jump into the Digest app (#124) resolves an
         explicit component before offering it, and from targetSdk 30 onward another package is
         invisible to that check unless declared here. Without this the offer would silently never
         appear on device, which is exactly the kind of failure that looks like the feature was never
         built. Narrow by design: one package, not QUERY_ALL_PACKAGES. -->
    <queries>
        <package android:name="com.ratta.supernote.knowledge" />
    </queries>


    <!-- Cleartext is enabled for one reason: a calibre / Calibre-Web server on the LAN speaks http
         (ADR-INKREAD-0016). The flag is application-wide and res/xml/network_security_config cannot
         narrow it to the private ranges, so the real rule — cleartext only to a local host — is
         enforced in HttpFetch.isCleartextAllowed and covered by CleartextPolicyTest. The config
         pins the hosts we ship (GitHub for the updater, Wiktionary) to HTTPS. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="InkRead"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/InkRead">

        <!-- Home screen + launcher: brand and entry points (Open / Library / Continue). -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The reader; launched from Home with a book / pick / resume extra (not exported). -->
        <activity
            android:name=".ReaderActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- App-level settings + help, launched from the Home gear (not exported). -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- The InkRead Daily front page (#66), launched from the Home masthead (not exported). -->
        <activity
            android:name=".DailyActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- OPDS library browser (ADR-INKREAD-0016, #175) — calibre / Calibre-Web, launched from the
             Home library card once a server is configured (not exported). -->
        <activity
            android:name=".OpdsActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Every book on the device, and the only place to remove one; launched from the Home
             shelf link (not exported). -->
        <activity
            android:name=".ShelfActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Self-update install callback (ADR-INKREAD-0014): receives the PackageInstaller session
             result and launches the OS install-confirmation intent. Internal only. -->
        <receiver
            android:name=".UpdateInstallReceiver"
            android:exported="false" />
    </application>
</manifest>
